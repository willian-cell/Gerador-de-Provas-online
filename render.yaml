services:
  - type: web
    name: gerador-de-provas-online
    env: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: DATA_DIR
        value: /var/data
      - key: DATABASE_URL
        sync: false        # paste from Neon.tech
      - key: GROQ_API_KEY
        sync: false        # your Groq key
    disk:
      name: wbo-data
      mountPath: /var/data
      sizeGB: 1
